{% extends "::base.html.twig" %}

{% block title %}Editer offre {id}{% endblock %}

{% block body %}
<h1>Editer l'offre 1245 :</h1>

    {{ form_start(offreForm, {attr: {novalidate:'novalidate'}}) }}
    {{ form_row(offreForm.titre) }}
    {{ form_row(offreForm.societe) }}
    {{ form_row(offreForm.lieu) }}
    {{ form_row(offreFormForm.date) }}
    {{ form_row(offreFormForm.type) }}
    {{ form_row(offreFormForm.description) }}
    {{ form_row(offreFormForm.categorie) }}
    <div>
        <button type="submit" class="btn btn-default">Modifier</button>
    </div>
    {{ form_end(contactForm) }}
    {#<form class="col-lg-12 col-md-12 col-sm-12 col-xs-12" name="editOffre" method="post" id="candidatureF" action="listoffre.html.twig">
        <div class="form-group">
            <label>Titre de l'offre</label>
            <input class="form-control" type="text" placeholder="titre de votre offre">
        </div>

        <div class="form-group">
            <label>Nom de la société</label>
            <input class="form-control" type="text" placeholder="nom de votre société">
        </div>

        <div class="form-group">
            <label>lieu</label>
            <input class="form-control" type="text" placeholder="Où se déroule votre offre">
        </div>

        <div class="form-group">
            <label>Date de publication</label>
            <select>

            </select>
        </div>

        <div class="form-group">
            <label>Motivation</label>
            <textarea form="candidatureF"></textarea>
        </div>
        <button type="submit" class="btn btn-primary col-lg-12 col-md-12 col-sm-12 col-xs-12">Candidature</button>
    </form> #}
{% endblock %}
